PORT=5000

# MongoDB Atlas URI (replace with your actual username & password if not already set in Render/Vercel)
MONGODB_URI=mongodb+srv://<your-username>:<your-password>@cluster0.5yseywp.mongodb.net/menwell1?retryWrites=true&w=majority

# JWT secret for future authentication features
JWT_SECRET=your_jwt_secret_here

# GROQ API Key (this is what GitHub blocked — do NOT hardcode this in code)
GROQ_API_KEY=your_groq_api_key_here

# Optional
NODE_ENV=development
PORT=5000

# MongoDB Atlas URI (replace with your actual username & password if not already set in Render/Vercel)
MONGODB_URI=mongodb+srv://<your-username>:<your-password>@cluster0.5yseywp.mongodb.net/menwell1?retryWrites=true&w=majority

# JWT secret for future authentication features
JWT_SECRET=your_jwt_secret_here

# GROQ API Key (this is what GitHub blocked — do NOT hardcode this in code)
GROQ_API_KEY=your_groq_api_key_here

# Optional
NODE_ENV=development
